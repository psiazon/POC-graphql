version: "3.9"
services:
  db:
    image: postgres:16
    container_name: loanapp_db
    restart: unless-stopped
    environment:
      POSTGRES_USER: postgres
      POSTGRES_PASSWORD: postgres
      POSTGRES_DB: loanapp
    ports:
      - "5432:5432"
    volumes:
      - loanapp_pgdata:/var/lib/postgresql/data
volumes:
  loanapp_pgdata:
